import { ViewFullImageDirective } from './view-full-image.directive';

describe('ViewFullImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewFullImageDirective();
    expect(directive).toBeTruthy();
  });
});
